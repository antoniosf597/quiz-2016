<!DOCTYPE html>
<html>
	<head>
	<meta charset="utf-8">
		<title>Quiz</title>
		<link rel="stylesheet" type="text/css" href="/stylesheets/style.css">
	</head>
	<body>
		<h2>Quiz: el juego de las preguntas</h2>

		
		<%= respuesta %>
		  <% if (respuesta === 'Incorrecto') { %>
       
          
        <a href="/quizes/<%= quiz.id %>?fallos=<%= cont %>">Intentalo otra vez</a>
      
        <% } else{ %>
		 <p>Has acertado!</p>
          
        <a href="/quizes/">Volver a preguntas</a>

		 <% } %>
	</body>
</html>